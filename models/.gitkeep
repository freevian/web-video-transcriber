# This file is intentionally left blank.
# It serves as a placeholder to ensure the 'models' directory is tracked by Git,
# even when it's empty, because the .gitignore file ignores all its other contents. 